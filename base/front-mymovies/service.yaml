apiVersion: v1
kind: Service
metadata:
  name: front-mymovies
  namespace: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: front-mymovies
  ports:
    - name: http
      port: 80
      targetPort: 3000
